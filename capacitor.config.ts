import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a04-lista-compras-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
